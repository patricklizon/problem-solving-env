{
  "name": "problem-solving-env",
  "version": "0.0.1",
  "description": "Environment for problem solving with TS and uvu",
  "private": true,
  "scripts": {
    "fix:format": "npm run test:format -- --write",
    "fix:lint": "npm run test:lint -- --fix",
    "postinstall": "husky install",
    "test": "uvu -r @swc-node/register --dir \"src/**/*.spec.ts$\"",
    "test:coverage": "c8 --clean --check-coverage --lines 95 --functions 95 --branches 95 npm test",
    "test:watch": "watchlist --eager src -- npm run test",
    "test:format": "prettier --check \"src/**/*.ts\"",
    "test:lint": "eslint --ext=ts src",
    "test:types": "tsc -p tsconfig.json --noEmit --skipLibCheck",
    "lint:staged": "lint-staged"
  },
  "lint-staged": {
    "*.ts": [
      "prettier --config .prettierrc --write",
      "eslint --fix"
    ]
  },
  "author": "Patryk Lizoń",
  "license": "ISC",
  "devDependencies": {
    "@swc-node/register": "^1.5.1",
    "@types/node": "^18.7.18",
    "@typescript-eslint/eslint-plugin": "^5.37.0",
    "@typescript-eslint/parser": "^5.37.0",
    "c8": "^7.12.0",
    "eslint": "^8.23.1",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-prettier": "^4.2.1",
    "eslint-plugin-sonarjs": "^0.15.0",
    "husky": "^5.0.9",
    "lint-staged": "^13.0.3",
    "prettier": "^2.7.1",
    "typescript": "^4.8.3",
    "uvu": "^0.5.6",
    "watchlist": "^0.3.1"
  }
}
